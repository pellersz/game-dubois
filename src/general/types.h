#ifndef TYPES_H
#define TYPES_H

typedef char offs;
typedef unsigned char byte;
typedef unsigned char u8;
typedef unsigned short word;
// TODO: remove this thing because stdint exists (caution uint_8 is still a character, and adding two of them together still becomes an int)

#endif
